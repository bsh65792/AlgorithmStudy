#include <iostream>
#include <string>

using namespace std;

int cnt[40] = { 0, };

int main() {

	string s;

	cin >> s;
  
	for (int i = 0; i < s.length(); i++) {
		cnt[s[i] - 'a']++;
	}


	for (int i = 0; i < 26; i++) {
		cout << cnt[i] << " ";
	}


	return 0;
}
